<% name = @student&.name
  @title = "#{name}さんの宿題修正" %>
<h1><%= @title %></h1>

<div id="generic-form">
<% if @student && @homework %>
  <%= form_with model: @homework, url: admin_student_homework_path(@student, @homework), method: :patch, authenticity_token: true do |f| %>
    <%= render "form", f: f %>
    <div class="buttons">
      <%= f.submit "修正" %>
      <%= link_to "キャンセル", :admin_students %>
    </div>
  <% end %>
<% else %>
  <p>学生または宿題記録が見つかりませんでした。</p>
<% end %>
</div>
