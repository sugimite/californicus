<% name = @student&.name
  @title = "#{name}さんの宿題入力" %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_with model: @homework, url: [ :admin, @student, :homeworks ] do |f| %>
    <%= render "form", f: f %>
    <div class="buttons">
      <%= f.submit "登録" %>
      <%= link_to "キャンセル", :admin_students %>
    </div>
  <% end %>
</div>
